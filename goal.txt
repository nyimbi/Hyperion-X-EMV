Develop a certification-worthy EMV Kernel from the capabilities defined in
docs/spec.md, incorporating the non-prescriptive engineering notes in
docs/eng_notes.md where they strengthen certification readiness.

Operating commitments:
- Commit completed, verified work regularly instead of allowing large batches
  of uncommitted work to accumulate.
- Keep a concise progress log of actions, evidence, and remaining risks.
- Be careful and meticulous, research current developments where useful, and
  keep the code clean and thoroughly documented.
- Preserve open certification blockers for evidence that requires external
  lab, scheme, device, CAPK, or official vector inputs.
- Push regular commits when a git remote is configured.

Current implementation focus:
- Close the code-review gaps identified on main at c8cb049.
- Replace secret-bearing certification bundle trust anchors with protected
  public verification material and keep private signing material out of browser
  storage and checked-in trust-anchor data.
- Make runtime policy truly data-driven by ensuring loaded callback timeout
  values are used by APDU transports and are verified by tests.
- Make capability coverage truthful: fixture/pending/empty vector data must not
  report as certification coverage.
- Enforce repository-controlled quality gates for 100% coverage candidates and
  deterministic data-bundle artifacts.
- Build strict, fail-closed import adapters and normalized integration/release-freeze
  bindings for real lab, scheme, CAPK, vector, device, and report artifacts when
  those external inputs become available.
- Keep documentation aligned with the implemented stack and remaining external
  certification dependencies.
- Create a guided certification wizard that prepares a user for a certifiable
  Hyperion kernel candidate, generates the data-bundle workspace, and keeps
  external approval boundaries explicit.

Progress log for this run:
- 2026-05-25: Started certification-gap closure run. Initial review findings:
  trust anchors ship signing secrets, runtime APDU timeouts ignore loaded bundle
  values, ODA vector coverage can overstate fixture status, CI coverage is
  measurement-only, bundle artifacts are not fully drift-gated, and docs/tooling
  need alignment.

Current verification status:
- README narrative update verified with `git diff --check`, `cargo fmt --check`, and `cargo test --test traceability_foundation -- --test-threads=1`.
- SoftPoS example verified with deterministic report/pre-lab/tooling drift checks, `cargo run --quiet --example krn_basic_softpos`, `cargo test --example krn_basic_softpos`, `cargo test --examples`, `cargo test --test traceability_foundation -- --test-threads=1`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, `scripts/coverage_100.sh`, and `git diff --check`.
- Data-bundle tooling documentation verified with report-pack JSON/Markdown/HTML drift checks, focused tutorial traceability test, full traceability test suite, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, `scripts/coverage_100.sh` at 27,594/27,594 source lines, formatting, and whitespace checks.
- Certification artifact import adapters verified with deterministic plan/report drift checks, focused importer and CLI tests, full unit/traceability/example tests, `cargo clippy --all-targets --all-features -- -D warnings`, `scripts/coverage_100.sh` at 28,177/28,177 source lines, formatting, and whitespace checks.
- Certification integration/release-freeze compiler verified with deterministic generated-doc drift checks, CLI smoke reports, focused importer tests, traceability tests, full unit/example tests, clippy with warnings denied, `scripts/coverage_100.sh` at 29,299/29,299 source lines, formatting, and whitespace checks.
- Passing: formatting, full unit/traceability/doc tests, example tests, clippy with warnings denied, enforced coverage script, coverage package audit, bundle validation/lint, deterministic report drift checks, and whitespace diff checks.
- Certification wizard verified with non-interactive smoke generation, deterministic pre-lab/tooling/report drift checks, full unit/traceability/doc tests via `cargo test`, all example tests via `cargo test --examples`, clippy with warnings denied, formatting, whitespace checks, and `scripts/coverage_100.sh` at 29,752/29,752 source lines = 100%.
- Coverage closure remains achieved for the repository-controlled Rust source line gate: `scripts/coverage_100.sh` passed with LCOV source line coverage at 29,752/29,752 = 100%. The generated package audits as `certification_candidate_unreviewed`; it still does not close external lab, scheme, device/L1, PCI/PED, CAPK-authority, official-vector, trace-acceptance, or final approval gates.


Current documentation status:
- Updated README.md so the public narrative clearly explains the data-driven
  kernel/bundle split, DataCraft's open-source rationale, bundle creation
  workflow, certification process, and the boundary between local evidence and
  external approval.
- Added `docs/tutorial/08-data-bundles-and-tools.md` as the comprehensive guide
  for building data bundles, using the workbench/TUI/CLI tools, validating and
  linting bundles, loading bundles through the kernel, freezing evidence, and
  avoiding insecure signing or trace practices.


Current example focus:
- Basic SoftPoS example added: `examples/krn_basic_softpos.rs` uses the
  Hyperion C ABI, contactless transaction parameters, data-driven certification
  bundle loading, mobile NFC APDU callbacks, CDCVM capability signaling, host
  authorization handoff, issuer authentication/final GAC, and redacted JSON
  output.

Next certification-readiness focus:
- Attach real external lab, scheme, acquirer, CAPK-authority, vector,
  device/L1, PCI/PED, trace-acceptance, and final approval artifacts as they
  become available, using format-specific adapters that emit the normalized
  integration manifest rather than bypassing the signed data-bundle boundary.
- Keep the 100% coverage gate green while closing external certification evidence attachments and lab/device/profile binding.
- Do not weaken the threshold or hide production code from coverage; use targeted tests or remove unreachable/dead code where appropriate.

- 2026-05-25: Added targeted coverage tests for FFI boundary/helper APIs, certification bundle lint/compile/report paths, coverage and trace-audit failure reporting, status-word classification, bitmap mutation safety, terminal capability cataloguing, contactless tuple validation, performance profile rejection modes, and report-plan JSON escaping. Full coverage gate now executes successfully through tests and fails only at the enforced 100% threshold with 96.25% line coverage and 920 missed lines.

- 2026-05-25: Added focused artifact-serializer, trace-policy, APDU helper, performance-profile, and AUC service-bit coverage. `cargo test --lib` now passes 410 tests, clippy is clean with warnings denied, and the coverage gate runs through all tests before failing only the enforced 100% threshold. Latest parsed LCOV source coverage: 96.92% (23,689/24,441 lines) with 752 missed source lines.

- 2026-05-25: Added C ABI fail-closed guard coverage, runtime contactless callback registration coverage, early transaction error-path coverage, CVM amount/condition/support edge coverage, and trace redaction escape coverage. `cargo fmt --check`, `cargo test --lib` (418 tests), `cargo clippy --all-targets --all-features -- -D warnings`, and `git diff --check` pass. The coverage gate executes all tests and fails only the enforced 100% threshold. Latest parsed LCOV source coverage: 97.40% (24,159/24,804 lines) with 645 missed source lines.

- 2026-05-25: Added internal C ABI helper coverage for select-builder null guards, generic APDU trace contexts, merchant/location DOL storage, terminal capability DOL tags, RNG callback failure, online authorization TLV encoding, transaction-currency comparison, record-reading short/end/TVR statuses, ODA error mapping, and offline outcome invalid states. Removed a redundant online authorization length check already enforced by the TLV appender. `cargo fmt --check`, `cargo test ffi --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.58% (24,392/24,998 lines) with 606 missed source lines.

- 2026-05-25: Added certification bundle self-attested testing-mode coverage, compile-report optional hash serialization coverage, and validator/parser rejection coverage for identifiers, CSV lists, clean strings, interfaces, hex lengths, callback timeouts, unknown fields, date parsing, and JSON helper accessors. `cargo fmt --check`, `cargo test cert_bundle --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.69% (24,527/25,108 lines) with 581 missed source lines.
- 2026-05-25: Added scheme-profile parser and CAPK component validator coverage for certification/test mode separation, missing and invalid schema metadata, profile source defaults, certification scope requirements, bounded string sets, optional numeric/date/hex fields, duplicate byte arrays, placeholder/dummy-key rejection, fixed vector sizing, unknown fields, strict JSON parsing, duplicate object keys, and null accessor behavior. `cargo fmt --check`, `cargo test config --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.80% (24,680/25,235 lines) with 555 missed source lines.
- 2026-05-25: Added FFI-focused coverage for transaction DOL encoding rejection, null merchant-location pointer rejection, ODA not-required/not-performed-required selection, missing CAPK mapping, invalid issuer certificate recovery, DDA-without-runtime dynamic signature failure, and CDA ICC certificate recovery failure. `cargo fmt --check`, `cargo test ffi --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.89% (24,882/25,419 lines) with 537 missed source lines.
- 2026-05-25: Added FFI profile-selection/contactless helper coverage for invalid selected state, bad scheme/AID indexes, invalid interface preference, contact/contactless interface mismatch, contactless kernel-type rejection, relay-resistance absence, relay transport timeout, and alternate-interface/terminate relay failure outcomes. `cargo fmt --check`, targeted FFI tests, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.93% (25,031/25,560 lines) with 529 missed source lines.
- 2026-05-25: Added parser edge coverage for malformed GPO/GAC top-level templates, missing GPO AIP in template 77, wrong-length GAC fixed fields, low-tag extra bytes, and overlong high-tag DOL fields. `cargo fmt --check`, `cargo test --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.96% (25,077/25,599 lines) with 522 missed source lines.
- 2026-05-25: Added variable-data boundary audit coverage for oversized source content, maximum finding overflow, invalid path forms, and explicit production/test source splitting. `cargo fmt --check`, targeted data-boundary tests, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, `scripts/coverage_100.sh`, and explicit LCOV generation passed except for the still-enforced 100% threshold. Latest parsed LCOV source coverage: 97.98% (25,111/25,630 lines) with 519 missed source lines.

- 2026-05-25: Closed small APDU/GPO/trace/issuer/record coverage gaps by replacing two API-unreachable APDU defensive branches with debug assertions over already-validated invariants and adding fail-closed parser/replay tests. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.02% (25,171/25,679 lines) with 508 missed source lines.

- 2026-05-25: Closed selection, issuer-script, record/cardholder, and coverage-metadata helper gaps with fail-closed tests for no-common-AID routing, selected ADF validation, issuer script empty/overflow cases, PAN/Track2 malformed boundaries, coverage parser scalar errors, and coverage package rewrite behavior. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.40% (25,538/25,952 lines) with 414 missed source lines.

- 2026-05-25: Closed FSM, trace-audit, coverage-package, and certification-evidence coverage gaps with tests for state codes/defaults/callback failure, malformed trace records, missing-hash audit JSON, TLV-count mismatches, JSON escaping, deterministic cleanup, unsupported attachment file types, mixed present/rejected evidence slots, and multi-entry serializers. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.40% (25,538/25,952 lines) with 414 missed source lines.

- 2026-05-25: Closed quality-manifest drift and variable-data production-split coverage gaps by refactoring manifest generation through testable case slices, adding fail-closed drift tests, and simplifying the production/test source split branch. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.42% (25,573/25,984 lines) with 411 missed source lines across src/ffi.rs, src/cert_bundle.rs, src/config.rs, and src/oda.rs.
- 2026-05-25: Closed certification bundle policy/serializer branches and FFI callback fixture buffer-boundary branches, including order-independent reset of shared callback fixture state. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.67% (25,984/26,333 lines) with 349 missed source lines across src/ffi.rs, src/config.rs, src/oda.rs, and src/cert_bundle.rs.
- 2026-05-25: Closed additional config and ODA parser/crypto edge coverage with direct fail-closed tests for scheme-profile arrays, TAA field parsing, lower/upper offline limit ordering, CDA profile consistency, CAPK source requirements, recovered certificate/signature parser boundaries, modular arithmetic helpers, and ODA vector coverage validators. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, scripts/coverage_100.sh test execution, and explicit LCOV generation. Latest parsed LCOV source coverage: 98.96% (26,606/26,886 lines) with 280 missed source lines across src/ffi.rs, src/oda.rs, src/cert_bundle.rs, and src/config.rs.

- 2026-05-25: Refactored FFI APDU/read-record/issuer-auth/script status handling into explicit testable helpers, added first/final GAC TC/AAC/referral/short-response coverage, covered callback fixture fallback responses, and made the coverage script run the shared-counter FFI harness with one deterministic test thread. Verification passed: cargo fmt --check, cargo test --lib, cargo clippy --all-targets --all-features -- -D warnings, git diff --check, non-enforcing scripts/coverage_100.sh execution, and explicit single-thread LCOV generation. Latest parsed LCOV source coverage: 99.56% (26,993/27,112 lines) with 119 missed source lines across src/ffi.rs, src/cert_bundle.rs, src/config.rs, and src/oda.rs.

- 2026-05-25: Closed the remaining repository-controlled Rust source line coverage gaps. Refactored FFI runtime transaction progression into explicit testable status/state helpers, added fail-closed selection/GPO/ODA/CDA/GAC/callback coverage, tightened certification bundle/config/ODA edge coverage, and updated the coverage package workflow to enforce the LCOV source line ledger while staging HTML plus LCOV evidence. Verification passed: cargo fmt --check, git diff --check, cargo clippy --all-targets --all-features -- -D warnings, scripts/coverage_100.sh, and cargo run --quiet --example krn_coverage_package_audit -- --root target/coverage --require-certification-candidate. Latest enforced LCOV source coverage: 100% (27,594/27,594 lines).

- 2026-05-25T09:50:07Z: Reworked README narrative to foreground Hyperion as an
  open-source, data-driven EMV kernel foundation; added DataCraft rationale,
  data-bundle authoring workflow, certification process layers, and explicit
  local-evidence versus external-approval boundaries.
