# Lab Submission Manifest – Hyperion EMV Kernel v6.0 (Draft for Certification)

## Product Information
- **Product Name:** Hyperion EMV Kernel
- **Version:** 6.0
- **Vendor:** Hyperion-X
- **Target Device:** Hyperion MP35P terminal and contactless readers pending device/L1 certification evidence
- **Submission Date:** [To be filled by lab]

## Certification Scope
- **EMV Level 2 Contact:** In scope for pre-certification hardening (Visa, Mastercard); final claim requires lab execution, signed approval evidence, and lab-supplied signed profiles before any Amex or Discover claim
- **EMV Level 2 Contactless (C‑8):** In scope for pre-certification hardening; final claim requires the unified kernel approval package and lab-supplied profile data
- **PCI PTS POI v7.0 alignment:** Alignment target pending PED integration statement and device certification evidence

## Attached Artifacts
- [x] Specification document (v6.0, this file)
- [x] Bitmap catalogue (`bitmap_catalogue.csv`) – canonical TVR/TSI symbolic bit mapping with RFU masks
- [x] Performance profile (`performance_profile.csv`) – product-level certification timing buckets and targets
- [x] TLV catalogue (`tlv_catalogue.csv`) – complete with 61 tags and required 10-column schema
- [x] State machine table (`state_machine.csv`) – expanded to 85 rows, properly quoted
- [x] ODA test vectors (`oda_test_vectors.json`) – syntactically validated with deterministic unit fixtures; lab-supplied SDA/DDA/CDA vectors still required for final certification
- [x] Scheme profiles (`scheme_profiles.cert.json`) – structured with valid hex lengths, contains deterministic TAA keys; actual CAPK values to be replaced by lab
- [x] Scheme profile dictionary (`scheme_profile_dictionary.md`) – human-readable review artifact generated by `cargo run --example krn_scheme_profile_dictionary`; summarizes AID/kernel/interface mappings, terminal capability/TTQ source boundaries, limits, CVM thresholds, action codes, and CAPK provenance without raw CAPK modulus disclosure
- [x] Requirement traceability matrices (`requirements_traceability.csv`, `requirements-traceability-matrix.csv`) – mapped to EMVCo test case categories with the compatibility copy retained in provenance
- [x] Certification open-issues register (`certification_open_issues.md`) – controlled list of external evidence blockers and closure criteria
- [x] Public standards watch (`standards_watch.md`) – records public EMVCo C-8 v1.1 / SB 325 drift and PCI PTS/PED evidence boundaries; licensed/lab reconciliation is still required before final C-8 or PCI PTS alignment claims
- [x] Trace identity metadata – masked APDU logs retain ABI version and signed profile version
- [x] Pre-lab APDU trace fixture (`prelab_apdu_trace_pack.jsonl`) – deterministic masked JSONL fixture generated by `cargo run --example krn_prelab_trace_pack`; covers trace-pack metadata, scenario expectations with FSM and APDU status actions, trace identity, PAN masking, Track 2 suppression, GENERATE AC cryptogram suppression, issuer-authentication/script status evidence, issuer-script retry status evidence, and APDU follow-up status evidence; full lab/test-tool trace pack remains pending
- [x] Pre-lab quality gate manifest (`prelab_quality_gates.json`) – deterministic local gate list generated by `cargo run --example krn_prelab_quality_gates`; includes explicit regeneration checks for the ABI conformance statement, masked trace fixture, quality manifest, and build-provenance command; formal coverage, integration, static-analysis, and fuzzing reports remain pending
- [x] Pre-lab decoder utility (`krn_emv_decode`) – controlled TLV, DOL, CVM-list, TVR, TSI, CID, SW1/SW2, and short APDU-envelope decoder with default payload suppression for lab-trace triage; not a substitute for full lab trace-pack evidence
- [x] C ABI APDU script adapter (`krn_cabi_script_adapter`) – deterministic request/response harness around `KrnRuntime` callbacks for pre-lab integration smoke tests; PC/SC, mobile NFC, and device drivers remain outside the kernel core
- [x] Reproducible build provenance – `cargo run --example krn_build_manifest -- src Cargo.lock Cargo.toml docs/...` emits canonical JSON with SHA-256 for every kernel source module, the manifest tool, profile dictionary generator, decoder utility, C ABI script adapter, lockfile, and controlled annexes
- [x] Source code (under NDA)
- [ ] Unit test report (≥95% coverage) – [to be attached]
- [ ] Integration test report (100% of EMV test plan) – [to be attached]
- [ ] Static analysis report (accepted for the Rust/product submission context) – [to be attached]
- [ ] Fuzzing report (1M iterations, no crashes) – [to be attached]
- [ ] PCI PTS integration statement (Model A – PED‑owned VERIFY) – [to be attached]
- [x] Conformance statement (ABI JSON) – `abi_conformance_statement.json` is generated by `cargo run --example krn_abi_conformance_statement` and matches `krn_get_conformance_statement_json` for the ABI build
- [ ] Conformance statement (signed EMVCo/lab template) – [to be attached]
- [ ] APDU trace logs (masked) for all test cases – full lab/test-tool trace pack [to be attached]

## Test Tool Configuration
- **EMVCo L2 Contact Test Tool:** Fime Eval4dev v3.2 (or equivalent)
- **EMVCo Contactless C‑8 Test Harness:** EMVCo‑certified v1.0 pending licensed reconciliation against public C‑8 v1.1 / SB 325 signals
- **Test Environment:** Hyperion test terminal with simulated cards (e.g., Fime MPS8)

## Certification Contact
- **Engineer:** [Name to be filled]
- **Email:** [email to be filled]

## Declaration
This document serves as a **certification baseline template**. Repository-controlled artifacts marked complete are structurally complete and syntactically correct, with deterministic executable fixtures where bundled. Lab-supplied cryptographic values, CAPKs, scheme certification vectors, device evidence, and approval artifacts must be supplied during the submission process. The kernel implementation is intended to align with EMV Contact Chip Specifications v4.4 and EMV Contactless Kernel Specification Book C‑8 v1.0, subject to licensed review, scheme validation, and laboratory approval. Public-drift reconciliation is required before any final C-8 claim.
